<nav class="navbar navbar-expand-lg navbar-light bg-white py-4 fixed-top">
  <div class="container">
    <a
      class="navbar-brand d-flex justify-content-between align-items-center order-lg-0"
      href="/"
    >
      <img src="images/shopping-bag-icon.png" alt="site icon" />
      <span class="text-uppercase fw-lighter ms-2">Wasi</span>
    </a>

    <div class="order-lg-2 nav-btns">
      <% if(currentUser){ %>
        <button type="button" class="btn position-relative">
          <a style="color: black;" href="/cart"><i class="fa fa-shopping-cart"></i></a>
          <span
            class="position-absolute top-0 start-100 translate-middle badge bg-primary"
            ><%=currentUser.cart.length%></span
          >
        </button>
     <% } %>
    </div>
    <div class="order-lg-2 nav-btns">
        <ul class="navbar-nav mx-auto">
          <% if(currentUser){ %>
            <li class="nav-item px-2 py-2">
              <a class="nav-link text-uppercase text-dark" href="/logout">Logout</a>
            </li>
         <% } else{ %>
            <li class="nav-item px-2 py-2">
              <a class="nav-link text-uppercase text-dark" href="/login">Login</a>
            </li>
        <%  } %>
          
        </ul>
    </div>

    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navMenu"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse order-lg-1" id="navMenu">
      <ul class="navbar-nav mx-auto text-center">
        <li class="nav-item px-2 py-2">
          <a class="nav-link text-uppercase text-dark" href="/">home</a>
        </li>
        <%if(currentUser && currentUser.role === 'seller' && currentUser.isVeryfiedSeller){ %>
        <li class="nav-item px-2 py-2">
          <a class="nav-link text-uppercase text-dark" href="/product/new"
            >New</a
          >
        </li>
        <%}%>
      </ul>
    </div>
  </div>
</nav>
